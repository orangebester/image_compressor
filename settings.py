# write path of source directory with images
src_dir_path = r"input"
# write path of directory where you want to store compressed images
dst_dir_path = r"output"
# specify a file size limit for image to be compressed
optimal_size = 1048576
# specify desirable file size format, which compressed image should have
optimal_format = "JPEG"
